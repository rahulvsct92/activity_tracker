<% flash.each do |name, msg| %>

	<% class_name = get_alert_class(name) %>
	<div class="alert alert-dismissible alert-<%= class_name %>">
	  <button class="close" type="button" data-dismiss="alert" aria-label="Close">
	  	<span aria-hidden="true"> ×</span>
	  </button>
	  <%= msg %>
	</div>

<% end %>